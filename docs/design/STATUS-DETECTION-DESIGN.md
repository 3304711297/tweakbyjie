# 状态检测设计

## 目标

为 tweakbyjie 增加优化状态识别能力，而不是只执行写入。

未来脚本可以显示：

- 当前系统值
- 推荐目标值
- 当前状态
- 是否需要修改

## 状态分类

### 已优化

当前值与目标值一致。

### 未优化

检测到默认或其他配置。

### 不适用

当前系统版本、硬件或功能不存在。

### 未知

无法可靠判断。

## 设计原则

- 检测功能独立于执行功能
- 不自动修改用户配置
- 不依赖单一注册表判断
- 高风险项目必须显示说明

## 后续实现

优先加入低风险检测：

- GameDVR 状态
- Game Mode 状态
- HAGS 状态
- Prefetch 状态
- Memory Compression 状态
- NVMe 驱动状态

高级项目保持手动确认。
