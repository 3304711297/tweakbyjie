# 开发维护说明

## 当前结构

`tweakbyjie.ps1` 当前采用单文件模块化设计：

- 通用注册表写入函数
- 备份与恢复模块
- 功能菜单模块
- 高级配置模块

## 后续维护方向

### 日志统一

计划增加统一日志函数，减少不同模块输出格式差异。

### 备份统一

现有备份文件继续保留，后续可逐步规范目录结构。

### 模块独立

新增功能优先以独立函数形式加入，避免影响已有优化逻辑。

## 修改原则

- 先文档，后代码
- 先验证，后合并
- 不随意改变已有目标值
- 高风险项目保持独立入口
